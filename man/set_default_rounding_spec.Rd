% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_default_rounding_spec.R
\name{set_default_rounding_spec}
\alias{set_default_rounding_spec}
\title{Set the default rounding specification used by periglue functions}
\usage{
set_default_rounding_spec(rspec)
}
\arguments{
\item{rspec}{a \code{rounding_specification} object created with \code{\link[=round_spec]{round_spec()}},
or \code{NULL} to clear the current default.}
}
\value{
invisibly returns the previous default rounding specification, or
\code{NULL} if none was set. This makes it easy to save and restore:
\code{old <- set_default_rounding_spec(new_spec)}.
}
\description{
Stores a \link{rounding_specification} object as the session-wide default used
by \code{\link[=peri_glue]{peri_glue()}} and \code{\link[=peri_value]{peri_value()}} when no \code{rspec} argument is supplied.
This is an alternative to setting individual \verb{periglue.*} options by hand.
Pass \code{NULL} to clear the stored default and revert to factory settings.
}
\examples{

# Set a custom default
rspec <- round_spec() |> round_using_decimal(digits = 3)
set_default_rounding_spec(rspec)

# peri_* functions now use this spec without needing rspec = ...
peri_value(pi)

# Clear the default to revert to factory settings
set_default_rounding_spec(NULL)

peri_value(pi)

}

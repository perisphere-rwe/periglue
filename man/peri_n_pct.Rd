% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peri_n_pct.R
\name{peri_n_pct}
\alias{peri_n_pct}
\title{Format a count and percentage pair}
\usage{
peri_n_pct(n, pct, rspec = NULL, form = "{n} ({pct}\%)")
}
\arguments{
\item{n}{a numeric vector of counts (non-negative integers).}

\item{pct}{a numeric vector of proportions, i.e., values in [0, 1].
These are multiplied by 100 before display.}

\item{rspec}{a \code{rounding_specification} object (see \link{round_spec}) used to
format the percentage. Counts are always shown as whole numbers. If
\code{NULL}, the current default rounding specification is used.}

\item{form}{a character string controlling how \code{n} and \code{pct} are combined.
Use \code{{n}} and \code{{pct}} as placeholders. Defaults to \code{"{n} ({pct}\%)"}.}
}
\value{
a character vector the same length as \code{n} and \code{pct}.
}
\description{
\code{peri_n_pct()} formats a count (\code{n}) and a proportion (\code{pct}) together as a
character string in the form \code{"n (xx.x\%)"}. This is the most common cell
format in demographic and summary tables.
}
\examples{

peri_n_pct(n = 123, pct = 0.452)

# vectorised
peri_n_pct(n = c(123, 456, 0), pct = c(0.123, 0.456, 0))

# NA proportions are replaced with miss_replace from the rounding spec
peri_n_pct(n = c(10, 0), pct = c(0.5, NaN))

# large counts respect the big_mark setting
rspec <- round_spec() |> format_big(mark = ",")
peri_n_pct(n = 12345, pct = 0.678, rspec = rspec)

# custom form
peri_n_pct(n = 123, pct = 0.452, form = "{n} [{pct}\%]")

}
\seealso{
Other table helpers: 
\code{\link{peri_ester}()},
\code{\link{peri_glue}()},
\code{\link{peri_pvalue}()},
\code{\link{peri_value}()}
}
\concept{table helpers}
